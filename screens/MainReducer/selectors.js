// dialog
export const getCategory = (state) =>
  state.explore.category;

export const getProduct = (state) => state.explore.product;

export const getAllProduct = (state) => state.explore.products;

export const getProfile = (state) => state.explore.profile;

export const getReviews = (state) =>
state.explore.reviews;

export const getResultProducts = (state) =>
  state.explore.resultProducts;

export const getUsernameToken = (state) =>
  state.explore.auth;
